$ = jquery = require('jquery'); //set jquery to both 'jquery' and '$'
//bootstrap expects jquery to be in the global namespace

var app = console.log("this is a test file in console log");

module.exports = app;